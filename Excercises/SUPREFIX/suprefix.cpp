#include <iostream>

using namespace std;

void nhapXL1(){
    string ch1,ch2,sub1,sub2;
    cin >> ch1 >> ch2;
    result = ch2.size();
    bool check=false;
    while (result>0 && check){
    	sub1 = ch1.substr(sub1.size()-result()-1,ch2)
	}
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    nhapXL1();
    return 0;
}